clear;
clc;

rng('default');

in1_observed = linspace(1,10,10)';
in2_observed = linspace(1,10,10)';

x_observed = [reshape(repmat(in2_observed, 1, 10)', [numel(repmat(in2_observed, 1, 10)), 1]), repmat(in1_observed, 10, 1)];

noise = rand(size(x_observed));

y_observed = x_observed(:,1) > x_observed(:,2)
